version: 3
# 対象ファイルパターンリスト
files:
  - pattern: .github/workflows/*.yml
  - pattern: .github/workflows/*.yaml
  - pattern: .github/actions/*/action.yml
  - pattern: .github/actions/*/action.yaml
min_age:
  # 更新するアクションのバージョンの最低経過日数
  value: 7
  always: true
